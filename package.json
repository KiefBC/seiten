{
  "name": "seiten",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "tailwind:build": "tailwindcss -i ./style/input.css -o ./style/output.css",
    "tailwind:watch": "tailwindcss -i ./style/input.css -o ./style/output.css --watch",
    "dev": "concurrently \"npm run tailwind:watch\" \"cargo leptos watch\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "tailwindcss": "^3.4.1"
  }
}
